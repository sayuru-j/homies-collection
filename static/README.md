# Static assets by site

| Path | Site | Routes |
|------|------|--------|
| `homielog/` | HomieLog (login + chat) | `/`, `/chat` |
| `stranger-danger/` | StrangerDanger (Omegle-style) | `/stranger-danger` |
| `shared/` | Cross-site JS (API client, icons) | — |

URLs are unchanged; only files on disk are grouped by product.
